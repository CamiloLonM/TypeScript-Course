interfaces ___ es parecido a la clase, se pueden usar las dos
 
 ## Que es un interfas?
    Es como una plantilla donde definimos las propiedades una funciones, pero a la hora --
    de implementarlas necesitamos que sean iguales a como se definieron

    DEFINE: palabra reservada <interface>

      *Esta define una serie de atributos (pilas Sin ser definidos), reglas y propiedades pudiendo generar variables
       nuevas del tipo de la interface ______ Los definidos Son OBLIGATORIOS ______

    El atributo y metodo no pueden ir juntos